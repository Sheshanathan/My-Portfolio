import { BarChart3, Braces, Database, PanelsTopLeft } from 'lucide-react'
import Reveal from '../components/Reveal.jsx'
import SectionHeading from '../components/SectionHeading.jsx'
import { highlights } from '../data/portfolioData.js'
const icons = [PanelsTopLeft, Braces, Database, BarChart3]
export default function About() {
  return <section id="about" className="content-section section-anchor"><SectionHeading index="01" eyebrow="About" title="Complete applications, not isolated screens." description="I care about the full path between a user action and a reliable result." /><div className="about-grid"><Reveal className="about-copy"><p className="lead">I&apos;m a Computer Science Engineering graduate specialising in Artificial Intelligence and Machine Learning, with a strong interest and hands-on experience in full-stack web development.</p><p>I enjoy understanding how complete applications work—from responsive interfaces and API requests to backend business logic and databases. My primary development stack is MERN, and I have built applications involving authentication, RBAC, CRUD operations, media management, REST APIs, employee workflows and analytics dashboards.</p><p>I also use Python, SQL and Power BI to analyse data and present useful business insights—an additional capability that complements my software development focus.</p></Reveal><div className="about-statement"><span className="big-mark">&lt;/&gt;</span><p>Frontend intuition.<br />Backend thinking.<br /><em>One product mindset.</em></p></div></div><div className="highlight-grid">{highlights.map((item, index) => { const Icon = icons[index]; return <Reveal key={item.title} className="highlight-item" delay={index * .07}><div><span>{item.number}</span><Icon size={21} /></div><h3>{item.title}</h3><p>{item.text}</p></Reveal> })}</div></section>
}
